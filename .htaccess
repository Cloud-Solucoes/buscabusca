Options -Indexes

RewriteEngine On
RewriteBase /buscabusca/

# Serve existing files/directories directly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route all requests to public/index.php
RewriteRule ^(.*)$ public/index.php [QSA,L]
